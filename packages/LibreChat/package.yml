name: LibreChat
category: others
type: 源码安装
description: Open-source AI chat platform supporting multiple AI providers
license: MIT
homepage: https://github.com/danny-avila/LibreChat

maintainer: lw520203
maintainer-email: 1823363429@qq.com

usage: |

  - 前置条件
    ```
    docker --version
    docker compose version
    # Docker >= 20.10

    sudo dnf install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin git
    sudo systemctl enable --now docker
    docker --version
    ```

  - 安装
    ```
    # 方式一：Docker Compose 部署（推荐）
    git clone https://github.com/danny-avila/LibreChat.git
    cd LibreChat
    cp .env.example .env
    nano .env          # 配置必要的 API Key
    docker compose up -d

    # 方式二：手动部署
    git clone https://github.com/danny-avila/LibreChat.git
    cd LibreChat
    npm install
    cp .env.example .env
    nano .env
    npm run build
    npm start
    ```

  - 基础配置
    ```
    # 编辑 .env 文件：
    # OpenAI
    OPENAI_API_KEY=sk-xxx

    # 本地 Ollama
    OLLAMA_BASE_URL=http://host.docker.internal:11434

    # 开放注册
    ALLOW_REGISTRATION=true
    ```

  - 快速使用
    ```
    # 1. 访问 http://localhost:3080
    # 2. 注册账号或使用默认管理员登录
    # 3. 在设置中添加 API Key
    # 4. 开始对话

    # 常用命令
    docker compose up -d          # 启动
    docker compose down           # 停止
    docker compose logs -f        # 查看日志
    docker compose pull           # 更新镜像
    ```

  - 验证安装
    ```
    curl http://localhost:3080
    docker compose logs -f
    ```

  - 卸载
    ```
    docker compose down -v   # -v 同时删除数据卷
    cd ..
    rm -rf LibreChat/
    ```